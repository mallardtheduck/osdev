[package]

name = kernel
description = BT/OS kernel, bootloader and tools
version = 0.1C
contentPath = content

[feature-kernel]

name = kernel.image
description = BT/OS kernel image
path = /
file = btos.bin
type = kernel.image
